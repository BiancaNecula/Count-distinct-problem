Tema 2 Structuri de Date
Necula Bianca 315CA

Task 1: Vector de frecventa
	-> se declara si se initializeaza cu 0 un vector de lungime 2000000.
	-> pentru fiecare aparitie a unui numar se incrementeaza cu 1 valoarea de pe indexul egal cu numarul respectiv.

Task 2: Hashtable cu open addressing
	-> se declara 2 structuri: una de de tip item cheie-valoare, cealalta de tip hashtable.
	-> valoarea va fi reprezentata chiar de frecventa aparitiilor fiecarui string.
	-> se citesc stringuri din fisier si pt fiecare se aplica functia de hash apoi se introduce in hashtable.
	-> se trateaza cazurile:
		-> daca elementul este nou, se introduce elementul in hashtable la indexul alocat de functia de hash.
		-> daca in locul respectiv se afla alt element, atunci se introduce in bucketul urmator, pana se gaseste unul liber.
		-> daca bucketurile pan ala sfarsit sunt ocupate, se reia de la indexul 0.
		-> daca elemntul exista deja in tabel, se incrementeaza valoarea de la indexul respectiv.
	-> dupa introducerea tuturor elementelor, se afiseaza stringurile si frecventa de aparitie si se elibereaza memoria.

Task 3: HyperLogLog
	-> se stabileste numarul de bucketuri m = 2^11 si declaram un vector de dimensiune m.
	-> citim fiecare numar din fisierul dat ca parametru pan ala sfarsitul fisierului.
	-> se aplica functia de hash pe fiecare numar si se extrag primii 11 biti (j) si restul bitilor (x) in variabile diferite.
	-> se numara primii biti 0 din x si se introduce nr in vectorul M verificand s afie mereu cea mai mare valoare posibila pt respectivul bucket pe pozitia j.
	-> se calculeaza media armonica dintre puterile lui 2 cu exponentul fiecare valoare memorata in vectorul M (Z).
	-> se afiseaza rezultatul final dupa formula: E = factorul de atenuare * m * m * Z * 2.
	-> factorul de atenuare calculat si folosit pt m = 2^11 a fost: 0,721.  

